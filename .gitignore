node_modules/
dist/
*.log
.DS_Store

# the vault is personal content, not part of the repo — ignore everything under
# notes/ but keep the folder itself so a fresh clone has somewhere to write.
notes/*
!notes/.gitkeep
